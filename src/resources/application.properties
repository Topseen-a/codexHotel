# MongoDB connection
spring.data.mongodb.uri=mongodb://localhost:27017/codexhotel
spring.data.mongodb.database=codexhotel

# Server port (default is 8080, set this explicitly)
server.port=8080

# App name
spring.application.name=codexHotel

# Logging level
logging.level.org.springframework.data.mongodb=INFO